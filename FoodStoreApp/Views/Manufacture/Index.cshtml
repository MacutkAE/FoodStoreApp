@model IEnumerable<FoodStoreApp.Models.Manufacture>

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">Производители</h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Manufacture" asp-action="Create" class="btn btn-primary">
            <i class="fas fa-plus"></i> &nbsp; Создать
            </a>
        </div>
    </div>

    <br /><br />

    @if (Model.Count() > 0)
    {
        <table class="table table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        Название
                    </th>
                    <th>
                        Описание
                    </th>
                    <th>
                        Рейтинг
                    </th>
                </tr>
            </thead>
            <tbody>
                @foreach (var manufacture in Model)
                {
                    <tr>
                        <td width="30%">@manufacture.Title</td>
                        <td width="40%">@manufacture.Description</td>
                        <td width="10%">@manufacture.Rating</td>
                        <td class="text-center">
                            <div class="w-75 btn-group" role="group">
                                <a asp-controller="Manufacture" asp-route-Id="@manufacture.Id" asp-action="Edit" class="btn btn-primary mx-2">
                                    Изменить
                                </a>
                                <a asp-controller="Manufacture" asp-route-Id="@manufacture.Id" asp-action="Delete" class="btn btn-danger mx-2">
                                    Удалить
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p> Нет действующих Производителей </p>
    }

</div>